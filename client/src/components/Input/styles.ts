import styled from "styled-components";

export const StyledInput = styled.input`
  box-sizing: border-box;
  font-size: inherit;
  padding: 20px;
  height: 50px;
  &:focus {
    outline: none;
  }
`;
